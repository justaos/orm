type DatabaseConfiguration = {
  hostname?: string;
  port?: number;
  database?: string;
  username?: string;
  password?: string;
  connect_timeout?: number;
};

export type { DatabaseConfiguration };
