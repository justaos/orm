enum NativeDataType {
  STRING = "string",
  NUMBER = "number",
  INTEGER = "integer",
  BOOLEAN = "boolean",
  DATE = "date",
  JSON = "json",
  TIMESTAMP = "timestamp"
}

export default NativeDataType;
